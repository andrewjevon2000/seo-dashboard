# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env — ignore every real env file; keep only the committed templates
.env
.env.*
!.env.example
!.env.production.example

# service account credentials — never commit
/secrets/
*-service-account.json
gcp-service-account*.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript / vitest
*.tsbuildinfo
/coverage
